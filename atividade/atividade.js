function cadastrarNota(aluno, nota){
let aluno = ("Digite seu nome:")    
let nota1 = ("Digite sua 1 nota:");
let nota2 = ("Digite sua 2 nota:");}

function calcularMedia(aluno){
return (nota1 + nota2) / 2}

function verificarAprovação(media){
let media = calcularMedia
}

function exibirResultado(aluno, media, aprovado){
  if(aprovado){media < 6 == ("Não aprovado!")}
else {media > 6 == ("Aprovado!")}
("Aluno "+aluno+", sua média é "+media+" e você foi:" +aprovado)
}
